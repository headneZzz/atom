export const clinicStorage = new webix.DataCollection({
	//url:"server/common.php?cmd=get_clinic&personell_id="this.personell_id,
	/*scheme:{
		$init:function(obj){
			const date = obj.date = new Date();
			if (obj.id > 17)
				obj.date.setDate(date.getDate() - 1);
			if (obj.id > 35)
				obj.date.setDate(date.getDate() - 1);
			obj.date.setHours(0,0,0,0);
		}
	}*/
});
